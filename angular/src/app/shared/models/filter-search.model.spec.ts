import { FilterSearchModel } from './filter-search.model';

describe('FilterSearchModel', () => {
  it('should create an instance', () => {
    expect(new FilterSearchModel()).toBeTruthy();
  });
});
